apiVersion: v1
kind: Secret
metadata:
  name: mongodb-secret
  namespace: event-booking
type: Opaque
stringData:
  MONGO_INITDB_ROOT_USERNAME: event_app
  MONGO_INITDB_ROOT_PASSWORD: event_app_password
  MONGO_INITDB_DATABASE: event_booking
